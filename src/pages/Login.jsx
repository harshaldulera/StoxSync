import React, { useState } from "react";
import { auth } from "../config/firebase";

const Login = () => {
    return (
        <div>
            <h1>Hello</h1>
        </div>
    );
};

export default Login;